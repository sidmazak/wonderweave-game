# Optional: absolute public origin for sitemap, robots, Open Graph, and JSON-LD.
# Defaults to https://wonderweave.app when unset. Change this to your real domain before launch.
NEXT_PUBLIC_SITE_URL=https://wonderweave.app
